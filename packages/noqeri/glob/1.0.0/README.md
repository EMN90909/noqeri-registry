# noqeri/glob

Allocation-free wildcard matching for Noqeri with `*`, `?`, backslash escaping, literal-prefix inspection, and a path-aware mode that prevents wildcards from silently crossing `/` path separators.

License: GPL-3.0-only.
