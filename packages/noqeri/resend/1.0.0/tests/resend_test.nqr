import "../src/resend.nqr"
function require(ok: bool): void { if !ok { let invalid: *u8 = 0 as *u8 print(invalid[0]) } }
require(resendEmailsUrl() == "https://api.resend.com/emails")
require(resendCredentialPresent("re_12345678"))
print("resend package tests: OK")
