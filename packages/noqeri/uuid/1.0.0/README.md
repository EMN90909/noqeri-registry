# noqeri/uuid

Portable UUID text validation and version inspection written entirely in Noqeri. It accepts the canonical 8-4-4-4-12 hexadecimal form.

```nqr
import package "noqeri/uuid"
let ok = uuidTextValid(bytes)
```
